# Exercice 1

Il faut écrire la fonction `strlength` qui renvoie la taille d'une chaine de caractère, comme `strlen` (sans utiliser `strlen`) **en passant le vecteur par pointeur explicite** dans cette fonction  :

@[strlength2]({"stubs": ["strlength.h"],"command": "sh /project/target/run.sh test_strlength","project": "strlength2"})
